    # 🧪 Debug: verify escalation
    - name: Check current effective user and permissions on /etc/sudoers.d
      become: true
      shell: |
        echo "Current user: $(whoami)"
        echo "Groups: $(id)"
        echo "Directory permissions for /etc/sudoers.d:"
        ls -ld /etc/sudoers.d
        echo "Can we write there?"
        test -w /etc/sudoers.d && echo "✅ Writable" || echo "❌ Not writable"
      register: sudo_debug

    - name: Display debug info
      debug:
        var: sudo_debug.stdout_lines
