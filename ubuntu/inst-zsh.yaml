---
- name: Install zsh
  hosts: "{{ target_group | d('all') }}"
  become: true

  tasks:
    - name: Install zsh
      ansible.builtin.apt:
        name: zsh
        state: present
        update_cache: true
      become: true
